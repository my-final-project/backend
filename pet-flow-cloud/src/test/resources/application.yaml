server:
  port: 8081
  error:
    whitelabel:
      enabled: true

spring:
  application:
    name: pet-flow-cloud
  jpa:
    open-in-view: true
    show-sql: false
    hibernate:
      ddl-auto: update
  profiles:
    active: test

#logging:
#  level: DEBUG
#  root: INFO
#  org.springframework: DEBUG
#  org.hibernate: ERROR
#  org.testcontainers: DEBUG

